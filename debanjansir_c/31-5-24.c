#include<stdio.h>
int checkstring(char *s1, char*s2)
{
    int i;
    for(i=0;s1[i]!='\0';i++)
}
int main()
{
    char s1[1000], s2[1000];
    gets(s1);
    gets(s2);
    checkstring(s1,s2);
    return 0;
}